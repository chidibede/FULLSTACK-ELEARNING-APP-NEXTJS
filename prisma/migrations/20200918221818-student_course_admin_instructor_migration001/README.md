# Migration `20200918221818-student_course_admin_instructor_migration001`

This migration has been generated by Chidi Enwereji at 9/18/2020, 11:18:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Course" (
"id" SERIAL,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"title" text   NOT NULL ,
"content" text   ,
"published" boolean   NOT NULL DEFAULT false,
"instructorId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Instructor" (
"id" SERIAL,
"email" text   NOT NULL ,
"name" text   ,
"username" text   NOT NULL ,
"password" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Student" (
"id" SERIAL,
"email" text   NOT NULL ,
"name" text   ,
"username" text   NOT NULL ,
"password" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."StudentOnCourses" (
"studentId" integer   NOT NULL ,
"courseId" integer   NOT NULL ,
PRIMARY KEY ("studentId","courseId")
)

CREATE TABLE "public"."Admin" (
"id" SERIAL,
"email" text   NOT NULL ,
"name" text   ,
"username" text   NOT NULL ,
"password" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Instructor.email_unique" ON "public"."Instructor"("email")

CREATE UNIQUE INDEX "Instructor.username_unique" ON "public"."Instructor"("username")

CREATE UNIQUE INDEX "Instructor.password_unique" ON "public"."Instructor"("password")

CREATE UNIQUE INDEX "Student.email_unique" ON "public"."Student"("email")

CREATE UNIQUE INDEX "Student.username_unique" ON "public"."Student"("username")

CREATE UNIQUE INDEX "Student.password_unique" ON "public"."Student"("password")

CREATE UNIQUE INDEX "Admin.email_unique" ON "public"."Admin"("email")

CREATE UNIQUE INDEX "Admin.username_unique" ON "public"."Admin"("username")

CREATE UNIQUE INDEX "Admin.password_unique" ON "public"."Admin"("password")

ALTER TABLE "public"."Course" ADD FOREIGN KEY ("instructorId")REFERENCES "public"."Instructor"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."StudentOnCourses" ADD FOREIGN KEY ("studentId")REFERENCES "public"."Student"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."StudentOnCourses" ADD FOREIGN KEY ("courseId")REFERENCES "public"."Course"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."admin"

DROP TABLE "public"."alembic_version"

DROP TABLE "public"."note"

DROP TABLE "public"."user"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200918221818-student_course_admin_instructor_migration001
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,56 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Course {
+  id           Int                @default(autoincrement()) @id
+  createdAt    DateTime           @default(now())
+  title        String
+  content      String?
+  published    Boolean            @default(false)
+  instructor   Instructor         @relation(fields: [instructorId], references: [id])
+  instructorId Int
+  students     StudentOnCourses[]
+}
+
+model Instructor {
+  id       Int      @default(autoincrement()) @id
+  email    String   @unique
+  name     String?
+  username String   @unique
+  password String   @unique
+  courses  Course[]
+}
+
+model Student {
+  id       Int                @default(autoincrement()) @id
+  email    String             @unique
+  name     String?
+  username String             @unique
+  password String             @unique
+  courses  StudentOnCourses[]
+}
+
+model StudentOnCourses {
+  student   Student @relation(fields: [studentId], references: [id])
+  studentId Int
+  course    Course  @relation(fields: [courseId], references: [id])
+  courseId  Int
+  @@id([studentId, courseId])
+}
+
+model Admin {
+  id       Int     @default(autoincrement()) @id
+  email    String  @unique
+  name     String?
+  username String  @unique
+  password String  @unique
+}
```


